import React from 'react';
import Layout from './Layout';

const Settings = () => {
    return (
        <Layout>
            <h1>Settings!</h1>
            <p>This is the settings page for Study Buddy. Select an option from the sidebar.</p>
        </Layout>
    );
};

export default Settings;