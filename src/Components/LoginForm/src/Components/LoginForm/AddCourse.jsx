import React from 'react';
import Layout from './Layout';

const AddCourse = () => {
    return (
        <Layout>
            <h1>Add a Course!</h1>
            <p>This is the add a course page for Study Buddy. Select an option from the sidebar.</p>
        </Layout>
    );
};

export default AddCourse;